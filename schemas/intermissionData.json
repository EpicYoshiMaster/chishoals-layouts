{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "showTime": {
            "type": "boolean",
            "description": "Whether to display the current time on the Omnibar",
            "default": true
		},
		"showEvent": {
			"type": "boolean",
			"description": "Whether to show the current event on the Omnibar",
			"default": true
		},
		"showLocation": {
			"type": "boolean",
			"description": "Whether to show the event location on the Omnibar",
			"default": true
		},
		"showFlavorText": {
			"type": "boolean",
			"description": "Whether to show custom flavor text on the Omnibar",
			"default": true
		},
		"flavorText": {
			"type": "string",
			"description": "Flavor Text to display on the Omnibar",
			"default": "See the current standings with !bracket"
		}
    },
    "required": ["showTime", "showEvent", "showLocation", "showFlavorText", "flavorText"]
}